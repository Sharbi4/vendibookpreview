Remove the "Sign up for free" button from the top navigation banner in `src/components/layout/Header.tsx` (lines 410-419). This is a standalone outline button that appears when no user is logged in. The "Become a Host" link and other authenticated actions remain untouched.